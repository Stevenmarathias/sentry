anthropic>=0.40.0
flask>=3.0.0
# Pass D1 — production WSGI server. The Flask dev server (`python sentry_web.py`)
# stays usable locally; gunicorn is what a host invokes via the Procfile
# (`web: gunicorn sentry_web:app`).
gunicorn>=21.2.0
opencv-python>=4.8.0
pillow>=10.0.0
openai-whisper>=20231117
sounddevice>=0.4.6
numpy>=1.24.0
reportlab>=4.0.0
# Pass 14 — YouTube import. yt-dlp is intentionally unpinned so a
# `pip install -U` picks up YouTube-frontend fixes; youtube-transcript-api
# is the primary caption path with a yt-dlp VTT fallback. ffmpeg is a
# system dependency (brew install ffmpeg).
yt-dlp>=2024.0.0
youtube-transcript-api>=0.6.0
